version: '3'
services:
  app:
    build:
      context: .
      dockerfile: ./docker/node/Dockerfile
    # volumes:
    #   - ./:/usr/src/app
    command: sh -c "yarn dev"
    ports:
      - "3000:3000"
